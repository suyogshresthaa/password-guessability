[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "pwguess"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = ["numpy"]

[tool.hatch.build.targets.wheel]
packages = ["src/pwguess"]
